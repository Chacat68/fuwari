---

import { getEntry, render } from "astro:content";
import BlogTimeline from "../components/BlogTimeline.astro";
import Markdown from "../components/misc/Markdown.astro"; // 使用通用 Markdown 组件，应用 custom-md 的排版样式
import MBTIOverview from "../components/misc/MBTIOverview.astro";
import { profileConfig, siteConfig } from "../config";
import MainGridLayout from "../layouts/MainGridLayout.astro";

const aboutPost = await getEntry("spec", "about");

if (!aboutPost) {
	throw new Error("About page content not found");
}

const { Content } = await render(aboutPost);

// About页面SEO信息
const aboutTitle = aboutPost.data.title
	? `${aboutPost.data.title} - ${siteConfig.title}`
	: `关于我 - ${siteConfig.title}`;
const aboutDescription =
	aboutPost.data.description ||
	"了解更多关于查猫CHACAT的信息，游戏开发者、内容创作者、手机摄影师";
const aboutKeywords = [
	"关于",
	"个人简介",
	"游戏开发",
	"内容创作",
	"摄影",
	"查猫",
	"CHACAT",
];

// 头像：从 about frontmatter 读取，可选；若未设置则退回站点 logo
type AboutFrontmatter = { avatar?: string; avatarAlt?: string };
const fm = aboutPost.data as AboutFrontmatter;
const avatarSrc = fm.avatar ?? profileConfig.avatar;
const avatarAlt = fm.avatarAlt ?? "个人头像";

// 头像两侧的固定短句（替代热门标签）
const phrases = [
	"记录生活里的小确幸",
	"用粤语唱出日常的温度",
	"打磨顺手可复用的小工具",
	"与AI协作探索创作边界",
	"以短诗捕捉转瞬的情绪",
	"用英文拓展表达与视野",
	"设计更高效的工作与学习流",
	"在音乐里寻找灵感与慰藉",
];
const leftTags = phrases.slice(0, 4);
const rightTags = phrases.slice(4, 8);
// 桌面端圆弧距离：按与头像圆周等距计算（上下更靠近头像，但整体同一圆弧）
// 头像在桌面端为 md:h-32/w-32，即半径 64px；与圆周保持额外间距 padding
const avatarRadius = 64; // px（桌面端）
const padding = 12; // 与头像圆周的额外安全距离
const arcRadius = avatarRadius + padding;
// 估算每条短句中心的垂直位置（相对头像中心）。短句高度~32px，间距~8px，步长约 40px
const yPositions = [-60, -20, 20, 60];
const arcOffsets = yPositions.map((y) => {
	const inside = Math.max(0, arcRadius * arcRadius - y * y);
	const xOnArc = Math.sqrt(inside);
	// 与头像边缘的水平差值（>=0）
	const delta = Math.max(0, xOnArc - avatarRadius);
	// 再加一个基础间距，避免过于贴近
	const base = 4;
	return Math.round(delta + base);
});
---

<MainGridLayout 
	title={aboutTitle} 
	description={aboutDescription}
	image={siteConfig.banner?.src}
	keywords={aboutKeywords}
>
    <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative mb-4 md:mb-8">
        <div id="about-container" class="card-base z-10 px-6 md:px-10 pt-8 pb-8 relative w-full">
            <!-- 头像与两侧热门标签（桌面端三列布局） -->
            <div class="hidden md:grid grid-cols-[1fr_auto_1fr] items-center gap-4 mb-2">
                <!-- 左列标签 -->
                <div class="justify-self-end flex flex-col items-end gap-2 min-h-[8rem]">
                    {leftTags.map((text, i) => (
                        <span class="inline-flex h-7 md:h-8 items-center gap-1 rounded-full px-3 text-xs font-medium text-white/80 bg-white/5 border border-white/10 hover:border-white/20 transition-colors whitespace-nowrap opacity-0 chip-left chip-arc-left" style={`--delay: ${i * 40}ms; --float-duration: ${3200 + i * 120}ms; --arc: ${arcOffsets[i]}px`}>
                            <span>{text}</span>
                        </span>
                    ))}
                </div>

                <!-- 中间头像 -->
                <div class="justify-self-center">
                    <img src={avatarSrc} alt={avatarAlt} loading="lazy" decoding="async"
                         class="h-28 w-28 md:h-32 md:w-32 rounded-full ring-1 ring-white/20 shadow-sm object-cover" />
                </div>

                <!-- 右列标签 -->
                <div class="justify-self-start flex flex-col items-start gap-2 min-h-[8rem]">
                    {rightTags.map((text, i) => (
                        <span class="inline-flex h-7 md:h-8 items-center gap-1 rounded-full px-3 text-xs font-medium text-white/80 bg-white/5 border border-white/10 hover:border-white/20 transition-colors whitespace-nowrap opacity-0 chip-right chip-arc-right" style={`--delay: ${i * 40}ms; --float-duration: ${3200 + i * 120}ms; --arc: ${arcOffsets[i]}px`}>
                            <span>{text}</span>
                        </span>
                    ))}
                </div>
            </div>

            <!-- 移动端：头像与标签分两行居中显示 -->
            <div class="md:hidden mb-2 flex items-center justify-center">
                <img src={avatarSrc} alt={avatarAlt} loading="lazy" decoding="async"
                     class="h-24 w-24 rounded-full ring-1 ring-white/20 shadow-sm object-cover" />
            </div>
            <div class="mb-3 text-center">
                <h1 class="text-lg md:text-xl font-semibold tracking-wide text-[#6E92C9] dark:text-[#86A3D5]">查猫CHACAT</h1> <!-- 将关于页标题字体从 text-xl/md:text-2xl 下调为 text-lg/md:text-xl，并采用图1相近的蓝色标题风格 -->
            </div>
            <!-- 角色标签展示（美化）：使用胶囊徽章风格，适配明暗主题变量 -->
            <div class="mb-3 flex flex-wrap items-center justify-center gap-2">
                <!-- 单个徽章：游戏开发者（参考图1的胶囊+描边风格） -->
                <span class="inline-flex items-center gap-1 rounded-full px-3 py-1 text-xs font-medium text-[#86A3D5] dark:text-[#9DB5E3] bg-white/5 dark:bg-white/5 border border-white/10 hover:border-white/20 transition-colors">
                    <span>🎮</span>
                    <span>游戏开发者</span>
                </span>
                <!-- 单个徽章：内容创作者 -->
                <span class="inline-flex items-center gap-1 rounded-full px-3 py-1 text-xs font-medium text-[#86A3D5] dark:text-[#9DB5E3] bg-white/5 dark:bg-white/5 border border-white/10 hover:border-white/20 transition-colors">
                    <span>✍️</span>
                    <span>内容创作者</span>
                </span>
                <!-- 单个徽章：手机摄影师 -->
                <span class="inline-flex items-center gap-1 rounded-full px-3 py-1 text-xs font-medium text-[#86A3D5] dark:text-[#9DB5E3] bg-white/5 dark:bg-white/5 border border-white/10 hover:border-white/20 transition-colors">
                    <span>📱</span>
                    <span>手机摄影师</span>
                </span>
            </div>

            <!-- 移动端短句（与桌面端一致） -->
            {phrases.length > 0 && (
                <div class="md:hidden mb-6 flex flex-wrap items-center justify-center gap-2">
                    {phrases.map((text, i) => (
                        <span class="inline-flex h-7 items-center gap-1 rounded-full px-3 text-xs font-medium text-white/80 bg-white/5 border border-white/10 hover:border-white/20 transition-colors whitespace-nowrap opacity-0 chip-up" style={`--delay: ${i * 60}ms; --float-duration: ${5200 + i * 240}ms`}>
                            <span>{text}</span>
                        </span>
                    ))}
                </div>
            )}
            <!-- 三个主题卡片：阅读 / 创作 / 跑步 -->
            <div class="grid grid-cols-3 gap-3 md:gap-6 mb-6">
                <!-- 卡片：阅读（纯文字布局，贴近图1） -->
                <div class="btn-card p-3 md:p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <!-- 纯文字布局：移除图标，仅保留标签、标题与描述 -->
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">所爱</div> <!-- 阅读卡片小标签：读屏可见，视觉隐藏 -->
                        <!-- 大标题：加粗、紧致行距、纯白色；与图1一致 -->
                        <h3 class="text-base md:text-2xl font-black leading-tight tracking-tight text-gray-900 dark:text-white">阅读</h3> <!-- 窄屏缩小为 text-base；浅色/深色主题自适应颜色 -->
                        <!-- 描述文案：方案B -->
                        <p class="mt-1 md:mt-2 text-xs md:text-base leading-6 md:leading-7 text-gray-600 dark:text-white/75">与好书相遇，和自己对话</p> <!-- 窄屏缩小描述字号与行高，并自适应主题颜色 -->
                    </div>
                </div>
                <!-- 卡片：创作（纯文字布局，贴近图1） -->
                <div class="btn-card p-3 md:p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">专注</div> <!-- 创作卡片小标签：读屏可见，视觉隐藏 -->
                        <h3 class="text-base md:text-2xl font-black leading-tight tracking-tight text-gray-900 dark:text-white">创作</h3> <!-- 窄屏缩小为 text-base；浅色/深色主题自适应颜色 -->
                        <!-- 描述文案：方案B -->
                        <p class="mt-1 md:mt-2 text-xs md:text-base leading-6 md:leading-7 text-gray-600 dark:text-white/75">把灵感落地成作品</p> <!-- 窄屏缩小描述字号与行高，并自适应主题颜色 -->
                    </div>
                </div>
                <!-- 卡片：跑步（纯文字布局，贴近图1） -->
                <div class="btn-card p-3 md:p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">日常</div> <!-- 跑步卡片小标签：读屏可见，视觉隐藏 -->
                        <h3 class="text-base md:text-2xl font-black leading-tight tracking-tight text-gray-900 dark:text-white">跑步</h3> <!-- 窄屏缩小为 text-base；浅色/深色主题自适应颜色 -->
                        <!-- 描述文案：方案B -->
                        <p class="mt-1 md:mt-2 text-xs md:text-base leading-6 md:leading-7 text-gray-600 dark:text-white/75">用脚步丈量日常</p> <!-- 窄屏缩小描述字号与行高，并自适应主题颜色 -->
                    </div>
                </div>
            </div>
            <!-- MBTI 概览条 -->
            <div class="mb-6 onload-animation" style="animation-delay: 100ms;">
                <MBTIOverview
                    typeBadge="INTJ-A"
                    bars={[
                        { leftLabel: 'INTROVERTED', rightLabel: 'EXTRAVERTED', leftPercent: 57, rightPercent: 43, barColor: 'from-sky-300 to-sky-500' },
                        { leftLabel: 'INTUITIVE', rightLabel: 'OBSERVANT', leftPercent: 68, rightPercent: 32, barColor: 'from-amber-300 to-amber-500' },
                        { leftLabel: 'THINKING', rightLabel: 'FEELING', leftPercent: 66, rightPercent: 34, barColor: 'from-emerald-300 to-emerald-500' },
                        { leftLabel: 'JUDGING', rightLabel: 'PROSPECTING', leftPercent: 53, rightPercent: 47, barColor: 'from-violet-300 to-violet-500' },
                        { leftLabel: 'ASSERTIVE', rightLabel: 'TURBULENT', leftPercent: 68, rightPercent: 32, barColor: 'from-rose-300 to-rose-500' },
                    ]}
                />
            </div>
            
            <!-- 博客历程时间线 -->
            <div class="mb-8 onload-animation" style="animation-delay: 150ms;">
                <BlogTimeline />
            </div>
            
			<!-- 原正文内容：替换为 Markdown 组件渲染，应用 custom-md 的排版样式 -->
			<!-- 关于博客展示模块（重构布局：左侧纵排标题 + 右侧内容区） -->
			<section class="mb-10">
				<div class="p-6 md:p-8 rounded-[var(--radius-large)] shadow-md border border-white/15 bg-white/[0.06] backdrop-blur-sm dark:bg-gray-800/60">
					<div class="grid grid-cols-1 md:grid-cols-[10rem_1fr] gap-6 md:gap-10 items-start">
						<!-- 左侧纵排标题，提供留白与层次 -->
						<aside class="hidden md:block md:sticky md:top-24 md:self-start">
							<div style="writing-mode: vertical-rl;" class="text-xl md:text-2xl font-extrabold tracking-[0.2em] text-white/90">关于博客</div>
						</aside>

						<!-- 右侧内容区 -->
						<div class="space-y-8">
							<!-- 分类主题：竖向卡片列表，减少拥挤感 -->
							<div>
								<h4 class="text-sm font-medium text-[#86A3D5]">分类主题</h4>
								<ul class="mt-3 grid grid-cols-1 sm:grid-cols-2 gap-3">
									<li class="rounded-xl border border-white/10 bg-white/5 p-3 md:p-4">
										<div class="text-sm md:text-base font-medium text-gray-900 dark:text-white">生活随笔</div>
										<p class="mt-1 text-xs md:text-sm text-white/65">日常感悟与思考片段</p>
									</li>
									<li class="rounded-xl border border-white/10 bg-white/5 p-3 md:p-4">
										<div class="text-sm md:text-base font-medium text-gray-900 dark:text-white">设计思考</div>
										<p class="mt-1 text-xs md:text-sm text-white/65">创作过程中的灵感与方法</p>
									</li>
									<li class="rounded-xl border border-white/10 bg-white/5 p-3 md:p-4">
										<div class="text-sm md:text-base font-medium text-gray-900 dark:text-white">人工智能</div>
										<p class="mt-1 text-xs md:text-sm text-white/65">探索 AI 时代的可能性</p>
									</li>
									<li class="rounded-xl border border-white/10 bg-white/5 p-3 md:p-4">
										<div class="text-sm md:text-base font-medium text-gray-900 dark:text-white">它山之石</div>
										<p class="mt-1 text-xs md:text-sm text-white/65">从他人智慧中汲取养分</p>
									</li>
									<li class="rounded-xl border border-white/10 bg-white/5 p-3 md:p-4 sm:col-span-2">
										<div class="text-sm md:text-base font-medium text-gray-900 dark:text-white">音乐欣赏</div>
										<p class="mt-1 text-xs md:text-sm text-white/65">那些触动心灵的旋律</p>
									</li>
								</ul>
							</div>

                            <!-- 写作缘起与更多页面：两列布局（左侧为"更多页面"，占比4:6） -->
                            <div class="grid grid-cols-1 md:grid-cols-[2fr_3fr] gap-6">
                                <div>
                                    <h4 class="text-sm font-medium text-[#86A3D5]">更多页面</h4>
                                    <div class="mt-3 grid grid-cols-1 gap-3">
                                        <a href="https://arch.chawfoo.com/" target="_blank" rel="noopener noreferrer" class="group flex items-center justify-between rounded-lg px-3 py-2 bg-white/5 border border-white/10 transition-colors">
                                            <span class="text-xs md:text-sm text-gray-800 dark:text-white">建筑指南</span>
                                            <span class="text-white/40 group-hover:text-white/70 transition-colors">↗</span>
                                        </a>
                                        <a href="https://neodb.social/users/Charliefoo/" target="_blank" rel="noopener noreferrer" class="group flex items-center justify-between rounded-lg px-3 py-2 bg-white/5 border border-white/10 transition-colors">
                                            <span class="text-xs md:text-sm text-gray-800 dark:text-white">Hobby</span>
                                            <span class="text-white/40 group-hover:text-white/70 transition-colors">↗</span>
                                        </a>
                                        <a href="https://work.chawfoo.com/" target="_blank" rel="noopener noreferrer" class="group flex items-center justify-between rounded-lg px-3 py-2 bg-white/5 border border-white/10 transition-colors">
                                            <span class="text-xs md:text-sm text-gray-800 dark:text-white">WorkPage</span>
                                            <span class="text-white/40 group-hover:text-white/70 transition-colors">↗</span>
                                        </a>
                                    </div>
                                </div>
                                <div class="space-y-3">
                                    <h4 class="text-sm font-medium text-[#86A3D5]">写作缘起</h4>
                                    <p class="text-xs md:text-sm leading-7 text-gray-700 dark:text-white/80">
                                        在这个数字花园里，我用文字记录着：生活随笔、设计思考、人工智能、它山之石与音乐欣赏。除此之外，我也整理了精心策划的建筑指南；把喜欢的电影、音乐、游戏珍藏在 Hobby；并在 WorkPage 呈现多年来的成长与作品。
                                    </p>
                                </div>
                            </div>
						</div>
					</div>
			</section>
            <Markdown class="markdown-content onload-animation prose-headings:scroll-mt-24 prose-img:rounded-xl prose-pre:rounded-lg prose-a:no-underline hover:prose-a:underline w-full">
                <Content />
            </Markdown>
        </div>
    </div>
</MainGridLayout>

<!-- 预加载主页资源，提升返回主页时的性能 -->
<script>
// 延迟预加载主页，避免影响当前页面性能
setTimeout(() => {
    // 预加载主页HTML
    const link = document.createElement('link');
    link.rel = 'prefetch';
    link.href = '/';
    document.head.appendChild(link);
    
    // 预加载主页的关键CSS（如果有的话）
    const cssLink = document.createElement('link');
    cssLink.rel = 'prefetch';
    cssLink.href = '/_astro/';
    cssLink.as = 'style';
    document.head.appendChild(cssLink);
}, 2000); // 2秒后开始预加载
</script>

<style>
@keyframes chipInLeft { from { transform: translateX(12px); opacity: 0 } to { transform: translateX(0); opacity: 1 } }
@keyframes chipInRight { from { transform: translateX(-12px); opacity: 0 } to { transform: translateX(0); opacity: 1 } }
@keyframes chipInUp { from { transform: translateY(6px); opacity: 0 } to { transform: translateY(0); opacity: 1 } }
@keyframes floatY { 0% { transform: translateY(0) } 50% { transform: translateY(-3px) } 100% { transform: translateY(0) } }

.chip-left { 
    will-change: transform, opacity; 
    animation: chipInLeft 320ms ease-out forwards, floatY var(--float-duration, 5200ms) ease-in-out infinite; 
    animation-delay: var(--delay, 0ms), calc(var(--delay, 0ms) + 320ms);
}
.chip-right { 
    will-change: transform, opacity; 
    animation: chipInRight 320ms ease-out forwards, floatY var(--float-duration, 5200ms) ease-in-out infinite; 
    animation-delay: var(--delay, 0ms), calc(var(--delay, 0ms) + 320ms);
}
.chip-up { 
    will-change: transform, opacity; 
    animation: chipInUp 280ms ease-out forwards, floatY var(--float-duration, 5200ms) ease-in-out infinite; 
    animation-delay: var(--delay, 0ms), calc(var(--delay, 0ms) + 280ms);
}
/* 圆弧分布（仅桌面端生效） */
@media (min-width: 768px) {
    .chip-arc-left { margin-right: var(--arc, 0px); }
    .chip-arc-right { margin-left: var(--arc, 0px); }
}
</style>