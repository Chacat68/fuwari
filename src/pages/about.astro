---

import { getEntry, render } from "astro:content";
import Markdown from "@components/misc/Markdown.astro";
import { siteConfig } from "../config";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";
import MainGridLayout from "../layouts/MainGridLayout.astro";
import { Icon } from "astro-icon/components";

const aboutPost = await getEntry("spec", "about");

if (!aboutPost) {
	throw new Error("About page content not found");
}

const { Content } = await render(aboutPost);

// About页面SEO信息
const aboutTitle = aboutPost.data.title
	? `${aboutPost.data.title} - ${siteConfig.title}`
	: `关于我 - ${siteConfig.title}`;
const aboutDescription =
	aboutPost.data.description ||
	"了解更多关于查猫CHACAT的信息，游戏开发者、内容创作者、手机摄影师";
const aboutKeywords = [
	"关于",
	"个人简介",
	"游戏开发",
	"内容创作",
	"摄影",
	"查猫",
	"CHACAT",
];
---

<MainGridLayout 
	title={aboutTitle} 
	description={aboutDescription}
	image={siteConfig.banner?.src}
	keywords={aboutKeywords}
>
    <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative mb-4 md:mb-8 mx-auto max-w-3xl md:max-w-4xl">
        <div id="about-container" class="card-base z-10 px-6 md:px-10 pt-8 pb-8 relative w-full">
            <!-- 个人名字展示：在标签上方显示作者姓名 -->
            <div class="mb-3 text-center">
                <h2 class="text-xl md:text-2xl font-semibold tracking-wide text-[var(--title-active)]">查猫CHACAT</h2>
            </div>
            <!-- 角色标签展示（美化）：使用胶囊徽章风格，适配明暗主题变量 -->
            <div class="mb-6 flex flex-wrap items-center justify-center gap-2">
                <!-- 单个徽章：游戏开发者 -->
                <span class="inline-flex items-center gap-1 rounded-full bg-[var(--btn-regular-bg)] px-3 py-1 text-xs font-medium text-[var(--btn-content)] hover:bg-[var(--btn-regular-bg-hover)] transition-colors">
                    <span>🎮</span>
                    <span>游戏开发者</span>
                </span>
                <!-- 单个徽章：内容创作者 -->
                <span class="inline-flex items-center gap-1 rounded-full bg-[var(--btn-regular-bg)] px-3 py-1 text-xs font-medium text-[var(--btn-content)] hover:bg-[var(--btn-regular-bg-hover)] transition-colors">
                    <span>✍️</span>
                    <span>内容创作者</span>
                </span>
                <!-- 单个徽章：手机摄影师 -->
                <span class="inline-flex items-center gap-1 rounded-full bg-[var(--btn-regular-bg)] px-3 py-1 text-xs font-medium text-[var(--btn-content)] hover:bg-[var(--btn-regular-bg-hover)] transition-colors">
                    <span>📱</span>
                    <span>手机摄影师</span>
                </span>
            </div>
            <!-- 三个主题卡片：阅读 / 创作 / 跑步 -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-6 mb-6">
                <!-- 卡片：阅读（纯文字布局，贴近图1） -->
                <div class="btn-card p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <!-- 纯文字布局：移除图标，仅保留标签、标题与描述 -->
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">所爱</div> <!-- 阅读卡片小标签：读屏可见，视觉隐藏 -->
                        <!-- 大标题：加粗、紧致行距、纯白色；与图1一致 -->
                        <h3 class="text-2xl md:text-3xl font-black leading-tight tracking-tight text-white">阅读</h3>
                        <!-- 描述文案：方案B -->
                        <p class="mt-2 text-base md:text-lg leading-7 text-white/75">与好书相遇，和自己对话</p>
                    </div>
                </div>
                <!-- 卡片：创作（纯文字布局，贴近图1） -->
                <div class="btn-card p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">专注</div> <!-- 创作卡片小标签：读屏可见，视觉隐藏 -->
                        <h3 class="text-2xl md:text-3xl font-black leading-tight tracking-tight text-white">创作</h3>
                        <!-- 描述文案：方案B -->
                        <p class="mt-2 text-base md:text-lg leading-7 text-white/75">把灵感落地成作品</p>
                    </div>
                </div>
                <!-- 卡片：跑步（纯文字布局，贴近图1） -->
                <div class="btn-card p-6 rounded-[var(--radius-large)] shadow-md border border-white/15 hover:border-white/25 transition-all duration-300 bg-white/[0.06] backdrop-blur-sm hover:shadow-lg hover:translate-y-[-1px] dark:bg-gray-800/60">
                    <div class="w-full">
                        <!-- 顶部小标签：改为 sr-only，视觉隐藏但保留可访问语义 -->
                        <div class="sr-only">日常</div> <!-- 跑步卡片小标签：读屏可见，视觉隐藏 -->
                        <h3 class="text-2xl md:text-3xl font-black leading-tight tracking-tight text-white">跑步</h3>
                        <!-- 描述文案：方案B -->
                        <p class="mt-2 text-base md:text-lg leading-7 text-white/75">用脚步丈量日常</p>
                    </div>
                </div>
            </div>
            <!-- 原正文内容：保持在卡片区域之后 -->
            <div class="prose prose-lg dark:prose-invert markdown-content max-w-none prose-headings:scroll-mt-24 prose-img:rounded-xl prose-pre:rounded-lg prose-a:no-underline hover:prose-a:underline">
                <Content />
            </div>
        </div>
    </div>
</MainGridLayout>

<style>
  /* 移除 About 页面标题右侧的锚点链接“#” */
  /* 仅针对本页面生效，避免影响站点其它页面 */
  #about-container :is(h1,h2,h3,h4,h5,h6) .anchor,
  #about-container :is(h1,h2,h3,h4,h5,h6) a.anchor {
    /* 隐藏由 rehype-autolink-headings 注入的锚点链接 */
    display: none !important;
  }
</style>