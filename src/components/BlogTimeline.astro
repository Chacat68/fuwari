---
// 博客历程时间线组件
interface TimelineEvent {
	year: string;
	period: string;
	title: string;
	description: string;
	icon: string;
}

const timelineEvents: TimelineEvent[] = [
	{
		year: "2008-2013",
		period: "萌芽期",
		title: "网易博客的温暖怀抱",
		description:
			"开始用文字记录生活的点点滴滴，那些真诚的文字不仅带来了几位珍贵的朋友，更重要的是，养成了观察生活、思考人生的习惯。",
		icon: "🌱",
	},
	{
		year: "2013-2016",
		period: "沉淀期",
		title: "迁至Lofter",
		description:
			"随着生活重心转移到上海，将创作阵地迁至Lofter。然而，都市生活的快节奏让我渐渐忙碌起来，创作也随之进入了短暂的休眠期。",
		icon: "💤",
	},
	{
		year: "2019",
		period: "重启期",
		title: "《付之一笑》诞生",
		description:
			"内心深处对表达的渴望重新被点燃。选择了Typlog作为新的起点，正式为博客取名《付之一笑》——既是对过往的释然，也是对未来的期许。",
		icon: "✨",
	},
	{
		year: "2023",
		period: "自由期",
		title: "拥抱技术自由",
		description:
			"拥抱了Notion+Vercel+Github的技术组合，终于摆脱了平台的束缚。这种自主掌控的感觉让我能够更纯粹地专注于内容本身。",
		icon: "🚀",
	},
	{
		year: "2024",
		period: "承诺期",
		title: "十年之约",
		description:
			"加入《十年之约》这个温暖的社群，与众多博主一起许下十年之约。这不仅是对读者的承诺，更是对自己持续创作的鞭策。",
		icon: "🤝",
	},
	{
		year: "2025",
		period: "进化期",
		title: "Astro重构",
		description:
			"采用Astro框架进行全面重构，在保持内容品质的同时，大幅提升了访问体验。技术的进步让表达变得更加流畅。",
		icon: "⚡",
	},
	{
		year: "未来",
		period: "探索期",
		title: "持续前行",
		description:
			"博客之路仍在继续，每一次技术迭代都是新的开始。期待在未来的岁月里，用文字记录更多美好的时光。",
		icon: "🔮",
	},
];
---

<div class="blog-timeline">
  <div class="timeline-header mb-8">
    <h3 class="text-xl md:text-2xl font-bold text-gray-900 dark:text-white mb-2">博客历程</h3>
    <p class="text-sm md:text-base text-gray-600 dark:text-white/75">我的博客之路始于2008年，至今已走过17个年头</p>
  </div>
  
  <div class="timeline-container relative">
    <!-- 时间线主线 -->
    <div class="timeline-line absolute left-4 md:left-1/2 top-0 bottom-0 w-0.5 bg-gradient-to-b from-blue-400 via-purple-500 to-green-400 transform md:-translate-x-0.5"></div>
    
    <div class="space-y-8">
      {timelineEvents.map((event, index) => (
        <div class={`timeline-item relative flex items-start ${index % 2 === 0 ? 'md:flex-row' : 'md:flex-row-reverse'}`}>
          <!-- 时间线节点 -->
          <div class={`timeline-node absolute left-4 md:left-1/2 w-3 h-3 rounded-full bg-white border-2 transform md:-translate-x-1.5 z-10 flex items-center justify-center ${event.year === "未来" ? "border-red-500 bg-red-500/20 animate-pulse" : "border-blue-500"}`}>
            <span class="text-xs">{event.icon}</span>
          </div>
          
          <!-- 内容卡片 -->
          <div class={`timeline-content ml-12 md:ml-0 md:w-5/12 ${index % 2 === 0 ? 'md:pr-8' : 'md:pl-8'}`}>
            <div class="bg-white/5 dark:bg-gray-800/60 backdrop-blur-sm rounded-lg p-4 md:p-6 border border-white/10 hover:border-white/20 transition-all duration-300 hover:shadow-lg hover:translate-y-[-2px]">
              <!-- 年份和时期标签 -->
              <div class="flex flex-wrap items-center gap-2 mb-3">
                <span class={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium border ${event.year === "未来" ? "bg-red-500/20 text-red-400 border-red-500/30" : "bg-blue-500/20 text-blue-400 border-blue-500/30"}`}>
                  {event.year}
                </span>
                <span class={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium border ${event.year === "未来" ? "bg-pink-500/20 text-pink-400 border-pink-500/30" : "bg-purple-500/20 text-purple-400 border-purple-500/30"}`}>
                  {event.period}
                </span>
              </div>
              
              <!-- 标题 -->
              <h4 class="text-base md:text-lg font-semibold text-gray-900 dark:text-white mb-2">
                {event.title}
              </h4>
              
              <!-- 描述 -->
              <p class="text-sm md:text-base text-gray-600 dark:text-white/75 leading-relaxed">
                {event.description}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  /* 优化动画性能，减少动画延迟 */
  .timeline-item {
    animation: fadeInUp 0.4s ease-out forwards;
    opacity: 0;
    transform: translateY(10px) translateZ(0); /* 强制硬件加速 */
    will-change: opacity, transform;
    backface-visibility: hidden; /* 减少重绘 */
  }
  
  .timeline-item:nth-child(1) { animation-delay: 0ms; }
  .timeline-item:nth-child(2) { animation-delay: 50ms; }
  .timeline-item:nth-child(3) { animation-delay: 100ms; }
  .timeline-item:nth-child(4) { animation-delay: 150ms; }
  .timeline-item:nth-child(5) { animation-delay: 200ms; }
  .timeline-item:nth-child(6) { animation-delay: 250ms; }
  
  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* 响应式调整 */
  @media (max-width: 767px) {
    .timeline-line {
      left: 1rem;
    }
    
    .timeline-node {
      left: 1rem;
    }
  }
  
  /* 时间线节点悬停效果 */
  .timeline-node:hover {
    transform: scale-110;
    box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.2);
  }
  
  /* 桌面端时间线节点悬停效果 */
  @media (min-width: 768px) {
    .timeline-node:hover {
      transform: translateX(-6px) scale(110%);
    }
  }
  
  /* 内容卡片悬停效果增强 - 优化性能 */
  .timeline-content .bg-white\/5 {
    transition: all 0.2s ease-out;
    will-change: transform, box-shadow;
    transform: translateZ(0); /* 强制硬件加速 */
    backface-visibility: hidden; /* 减少重绘 */
  }
  
  .timeline-content .bg-white\/5:hover {
    background: rgba(255, 255, 255, 0.08);
    box-shadow: 0 8px 20px -5px rgba(0, 0, 0, 0.1), 0 8px 8px -5px rgba(0, 0, 0, 0.04);
  }
  
  /* 移动端性能优化 */
  @media (max-width: 768px) {
    .timeline-item {
      animation-duration: 0.3s; /* 缩短动画时间 */
    }
    
    .timeline-content .bg-white\/5 {
      will-change: auto; /* 移动端减少will-change使用 */
    }
  }
  
  /* 页面切换时暂停动画 */
  html.is-changing .timeline-item {
    animation-play-state: paused !important;
    will-change: auto !important;
  }
</style>
